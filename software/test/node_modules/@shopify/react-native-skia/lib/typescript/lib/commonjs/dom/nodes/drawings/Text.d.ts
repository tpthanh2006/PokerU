export const __esModule: boolean;
export class TextNode extends _DrawingNode.JsiDrawingNode {
    constructor(ctx: any, props: any);
    deriveProps(): any;
    draw({ canvas, paint }: {
        canvas: any;
        paint: any;
    }): void;
}
export class TextPathNode extends _DrawingNode.JsiDrawingNode {
    constructor(ctx: any, props: any);
    deriveProps(): any;
    draw({ canvas, paint }: {
        canvas: any;
        paint: any;
    }): void;
}
export class TextBlobNode extends _DrawingNode.JsiDrawingNode {
    constructor(ctx: any, props: any);
    deriveProps(): null;
    draw({ canvas, paint }: {
        canvas: any;
        paint: any;
    }): void;
}
export class GlyphsNode extends _DrawingNode.JsiDrawingNode {
    constructor(ctx: any, props: any);
    deriveProps(): any;
    draw({ canvas, paint }: {
        canvas: any;
        paint: any;
    }): void;
}
import _DrawingNode = require("../DrawingNode");
